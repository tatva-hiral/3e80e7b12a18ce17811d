import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  fromDivider: {maxWidth: '80%'},
  flagImageContainer: {width: 50, height: 50},
  flagImage: {width: '100%', height: '100%', resizeMode: 'contain'},
});

export default styles;
